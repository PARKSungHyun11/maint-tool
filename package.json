{
  "name": "maint-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Aircraft maintenance dispatch date and time calculator.",
  "scripts": {
    "verify": "node tools/check-jsx.js && node tests/run.js",
    "test": "node tests/run.js",
    "check:jsx": "node tools/check-jsx.js",
    "icons": "node tools/build-icons.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "optionalDependencies": {
    "playwright": "^1.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
